fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0     # loads .env configuration
numpy>=1.26
pandas>=2.2
pyarrow>=16
scipy>=1.13
lifelines>=0.28
python-multipart>=0.0.9
boto3>=1.34          # only needed for the cloud (object) backend
# dev / test
pytest>=8.0
httpx>=0.27
moto[s3]>=5.0          # in-process S3 mock used by the cloud-path test
